import React from 'react';
import {SafeAreaView} from 'react-native';

import Router from './routes/router';
const App = () => {
  return (
    <SafeAreaView>
      <Router />
    </SafeAreaView>
  );
};

export default App;
